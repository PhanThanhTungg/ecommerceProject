mixin formSearch(keySearch)
  form.d-flex.formSearch
    input(
      type="text"
      placeholder="Nhập từ khóa"
      name="keyword"
      class="form-control"
      value=keySearch
    )
    button(
      class="btn btn-success"
      type="submit"
    ) Tìm